.. _installation:

Installation
============

If not already done, please install *Python* and *Sphinx* first. *dox_trace* itself does not need to
be installed, but has to be available in the workspace, e.g. by cloning or copying it.

.. note::

    This page gets updated soon when *dox_trace* is available as package on PyPI.
