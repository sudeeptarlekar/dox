.. _backward_compatibility:

Backward Compatibility
======================

test_setups
-----------

``test_setups`` is deprecated and replaced by ``verification_methods``.
This deprecation is currently backward compatible.

Dim will automatically replace ``test_setups`` with
:ref:`verification_methods <verification_methods>` while formatting and exporting files.
